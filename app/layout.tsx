import './globals.css'; import type { Metadata } from 'next'; import { SITE } from '@/lib/config'; import { Header } from '@/components/Header'; import { Footer } from '@/components/Footer'; export const metadata:Metadata={title:`${SITE.name} | Washington State`,description:SITE.description,openGraph:{title:SITE.name,description:SITE.description,images:['/og.jpg'],type:'website'},twitter:{card:'summary_large_image',title:SITE.name,description:SITE.description}}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang='en'><body><Header/><main>{children}</main><Footer/></body></html>}