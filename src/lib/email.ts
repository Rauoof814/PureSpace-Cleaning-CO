import nodemailer from 'nodemailer'; export async function sendOwnerEmail(subject:string,html:string){ if(!process.env.SMTP_HOST){console.warn('SMTP not configured. Skipping email send.'); return {skipped:true};} const transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:false,auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined} as any); await transporter.sendMail({from:process.env.SMTP_FROM||'no-reply@purespace.local',to:process.env.OWNER_EMAIL||'owner@example.com',subject,html}); return {ok:true};}