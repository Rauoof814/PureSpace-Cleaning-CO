import Link from 'next/link'; import data from '@/content/services.json'; import { Reveal } from '@/components/Reveal'; export function ServiceGrid(){ const items=Object.values(data as any); return (<div className='grid gap-4 md:grid-cols-2 lg:grid-cols-3'>{items.map((s:any,i:number)=>(<Reveal key={s.slug} delay={i*0.05}><div className='card h-full flex flex-col'><h3 className='text-xl font-semibold'>{s.title}</h3><p className='opacity-80 mt-2'>{s.intro}</p><div className='mt-auto'><Link className='btn btn-outline mt-4' href={`/services/${s.slug}`}>Explore</Link></div></div></Reveal>))}</div>); }