'use client'; import { useEffect,useState } from 'react'; function useCounter(target:number,duration=1200){const [v,setV]=useState(0); useEffect(()=>{const s=performance.now(); const step=(t:number)=>{const p=Math.min(1,(t-s)/duration); setV(Math.floor(target*p)); if(p<1) requestAnimationFrame(step)}; requestAnimationFrame(step)},[target,duration]); return v;} export function KPIs(){ const a=useCounter(250),b=useCounter(98),c=useCounter(1200); return (<div className='grid grid-cols-3 gap-4 text-center'><div className='card'><div className='text-3xl font-bold'>{a}+</div><div>Commercial Sites</div></div><div className='card'><div className='text-3xl font-bold'>{b}%</div><div>On‑Time Arrival</div></div><div className='card'><div className='text-3xl font-bold'>{c}+</div><div>Projects Completed</div></div></div>);}