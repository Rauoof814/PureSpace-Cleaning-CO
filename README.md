# PureSpace Cleaning Co. — Pro v3

Run:

```bash
npm install
npm run dev
# prod
npm run build && npm run start
```

Supabase: run `server/schema.sql` then fill `.env.local`.
SMTP: fill SMTP_* and OWNER_EMAIL.
Content: `src/content/*.json`.
